<b><%= link_to portfolio_item.title, portfolio_show_path(portfolio_item) %></b>
<p><%= portfolio_item.subtitle %></p>
<p><%= portfolio_item.body %></p>
<%= image_tag portfolio_item.main_image unless portfolio_item.main_image.nil?%></br>
<%= link_to "Show", portfolio_show_path(portfolio_item) %></br>
<%= link_to "Edit", edit_portfolio_path(portfolio_item) %></br>
<%= link_to 'Destroy', portfolio_path(portfolio_item),
            method: :delete,
            data: { confirm: 'Are you sure?' } %></br>